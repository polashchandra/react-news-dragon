/* eslint-disable react/prop-types */
import React from 'react';
import PropTypes from 'prop-types';
const Media = ({newse}) => {
    const {image_url}=newse;
    return (
        <div>
            
        </div>
    )
};

export default Media;